apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gatekeeper
  namespace: gatekeeper
spec:
  dnsNames:
    - gatekeeper.src.skach.org
  secretName: gatekeeper.src.skach.org
  issuerRef:
    name: letsencrypt-gatekeeper-issuer
    kind: ClusterIssuer