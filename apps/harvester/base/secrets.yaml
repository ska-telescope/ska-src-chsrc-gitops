---
#TODO: for now secrets have been installed by hand from a private repo (helm install -f secrets-values.yaml -n harvester panda-secrets secrets/, ran from the root of cloned repo: https://github.com/PanDAWMS/panda-k8s/tree/main)