apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harvester
  namespace: harvester
spec:
  dnsNames:
    - harvester.dev.skach.org
  secretName: harvester.dev.skach.org
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer