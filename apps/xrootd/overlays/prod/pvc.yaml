apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: xrootd-cephfs-pvc
  namespace: xrootd
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
  storageClassName: ceph-corbo-cephfs-retain