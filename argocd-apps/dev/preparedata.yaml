apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: preparedata
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://gitlab.com/ska-telescope/src/deployments/chsrc/ska-src-chsrc-services-cd.git
      targetRevision: choc-178-preparedata
      ref: argorepo
      path: apps/preparedata/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: preparedata
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
